// Single source of truth for the visible brand.
// `loja` is the store name (the owner of the site) shown prominently.
// `maker` is just the credit for who built the site (tiny "powered by").
export const BRAND = {
  loja: 'Compras da Ca e Dani',
  maker: 'coreiq',
} as const
